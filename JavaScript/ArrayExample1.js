// declare array
const numList1 = [1, 4, 5, 6, 2, 3, 1, 7, 6, 8];

// initialize 'sum' variable
let sum = 0;

// iterate through array and return sum of elements
for (let i = 0; i < numList1.length; i++){
  sum = sum + numList1[i];
}
// print sum to console
console.log("sum: " + sum);

